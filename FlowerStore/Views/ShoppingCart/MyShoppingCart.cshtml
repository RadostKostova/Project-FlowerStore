@using FlowerStore.Core.ViewModels.Cart
@model CartViewModel

@{
    ViewBag.Title = "Shopping Cart";
}

<h2>@ViewBag.Title</h2>
<br />

@* <div class="card text-center">
    <div class="row g-0">
        <div class="col-md-8 cart">
            <div class="title">
                <div class="row">
                    <div class="col align-self-center text-right text-muted">Products in cart: @Model.ProductsCounter products</div>
                </div>
            </div>
            <div class="shopping-cart-container">
            <div class="row border-top border-bottom">
                @if (Model.ProductsCounter == 0)
                {
                    <h3 class="text-center fst-italic">Your cart is empty.</h3>
                }
                else
                {
                    @foreach (var product in Model.ShoppingCartProducts)
                    {

                        <div class="row main align-items-center border-bottom">
                            <div class="col-2">
                                <img class="img-fluid" src="@product.ImageUrl">
                            </div>
                            <div class="col">
                                <div class="row text-muted">@product.Category</div>
                                <div class="row">@product.Name</div>
                            </div>
                            <div class="col">
                                <div class="quantity-form">
                                    <button class="quantity-btn" id="minus-btn">-</button>
                                    <input type="number" class="quantity-input" id="quantity" value="1" min="1">
                                    <button class="quantity-btn" id="plus-btn">+</button>
                                </div>
                            </div>
                            <div class="col">
                                Product price: @product.UnitPrice BGN
                                <span class="close">&#10005;</span>
                            </div>
                        </div>
                    }
                }
            </div>
            </div>


            <div class="back-to-shop">
                <a href="#">&leftarrow;</a>
                <span class="text-muted">Back to shop</span>
            </div>
        </div>

        <div class="col-md-4 summary border-start">
            <div class="text-center">
                <h5><b>Summary</b></h5>
            </div>
            <hr>
            <div class="row">
                <div class="col" style="padding-left:0;">Items: @Model.ProductsCounter</div>
                <div class="col text-right">@Model.TotalPrice BGN</div>
            </div>
            <form style="padding: 1rem">
                <p>SHIPPING</p>
                <select>
                    <option class="text-muted">Standard Delivery - 5.00 BGN</option>
                </select>
                <p style="margin-top: 1rem">DO YOU HAVE ANY CODE?</p>
                <input id="promoCode" placeholder="Enter your code here">
            </form>
            <div class="row" style="padding: 2vh 0;">
                <div class="col">TOTAL PRICE</div>
                <div class="col text-right">@Model.TotalPrice BGN</div>
            </div>
            <button class="btn">CHECKOUT</button>
        </div>
    </div>
</div> *@

@* -------------------------------------------------------- *@
<section class="h-100" style="background-color: #eee;">
    <div class="container h-100 py-5">

        <h2 class="fst-italic text-black text-center mb-0">@ViewBag.Title</h2>

        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col-10">

                <div class="col">
                    <p class="mb-0">Items: @Model.ProductsCounter</p>
                </div>

                <div class="row d-flex justify-content-between align-items-center mb-4">
                    <div class="col text-right">
                        @* <form asp-controller="ShoppingCart" asp-action="UpdateCart" method="post">
                        <input type="hidden" asp-for="@Model.Id" name="productId" />
                        <button type="submit" id="refreshButton" class="btn btn-success">
                        <i class="bi bi-arrow-clockwise"></i>
                        </button>
                        </form> *@
                    </div>
                </div>

                @if (Model.ProductsCounter == 0)
                {
                    <h3 class="text-center fst-italic">Your cart is empty.</h3>
                    <br />
                }
                else
                {
                    @foreach (var product in Model.ShoppingCartProducts)
                    {
                        <div class="card rounded-3 mb-4">
                            <div class="card-body p-4">
                                <div class="row d-flex justify-content-between align-items-center">

                                    <div class="col-md-2 col-lg-2 col-xl-2">
                                        <img class="img-fluid rounded-3" src="@product.ImageUrl" alt="Product Image">
                                    </div>

                                    <div class="col-md-3 col-lg-3 col-xl-3">
                                        <p class="lead fw-normal mb-2">@product.Name</p>
                                        <p class="m-0"><span class="text-muted">Category: </span>@product.Category</p>
                                        <p><span class="text-muted">Price: </span>@product.UnitPrice</p>
                                    </div>

                                    <div class="col-md-2 col-lg-2 col-xl-2 d-flex">
                                        <div class="quantity-btn">
                                            <button type="button" class="btn btn-sm btn-secondary" id="decrement">-</button>
                                            <input type="number" class="form-control form-control-sm" id="productQuantity" name="productQuantity" value="@product.Quantity" readonly>
                                            <button type="button" class="btn btn-sm btn-secondary" id="increment">+</button>
                                        </div>
                                    </div>

                                    <div class="col-md-2 col-lg-2 col-xl-2 offset-lg-0">
                                        <h5 class="mb-0">@product.TotalPrice BGN</h5>
                                    </div>

                                    <div class="col-md-1 col-lg-1 col-xl-1 d-flex align-items-center justify-content-center">
                                        <div class="btn-group-vertical vertical-btn-group" role="group">

                                            @* <form asp-controller="ShoppingCart" asp-action="UpdateCart" method="post">
                                                <input type="hidden" asp-for="@product.ProductId" class="productId" name="productId" />
                                                <button type="submit" id="refreshButton" class="btn btn-success">
                                                    <i class="bi bi-arrow-clockwise"></i>
                                                </button>
                                            </form> *@

                                            <form asp-controller="ShoppingCart" asp-action="RemoveFromCart" method="post">
                                                <input type="hidden" asp-for="@product.ProductId" id="prodId" name="productId" />
                                                <button type="submit" class="btn btn-danger">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </div>

                                </div>
                            </div>
                        </div>
                    }
                }


                <div class="card mb-4">
                    <div class="card-body p-4 d-flex flex-row">
                        <div class="form-outline flex-fill">
                            <label class="form-label fst-italic" for="discountCode">Do you have any discount code?</label>
                            <input type="text" id="discountCode" name="discountCode" class="form-control form-control-lg" />
                        </div>
                        <button type="button" class="btn btn-outline-warning btn-lg ms-3">Apply</button>
                    </div>
                </div>

                <div class="card">
                    <div class="card-body d-flex justify-content-between">
                        <a asp-controller="Product" asp-action="Catalog" class="btn btn-warning btn-lg">Back to Shopping</a>
                        <a asp-controller="Order" asp-action="PlaceOrder" class="btn btn-warning btn-lg">Proceed to Pay</a>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>
